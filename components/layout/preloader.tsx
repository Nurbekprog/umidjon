"use client";

// Preloader — GSAP signal-line draw → logo clip-path reveal → curtain split exit
// Re-exports Loader for backwards compatibility with existing dynamic imports.
export { Loader as Preloader, Loader } from "./loader";
